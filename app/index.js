// Use `require('./file.js')` to use another JavaScript file or dependency
// var counter = require('./counter.js')

document.body.innerHTML = '<div>javascript: app/index.js</div>' + document.body.innerHTML
